# ボンガレコード → OBS 配信連携 セットアップ手順

Firebaseの登録もHTMLの編集も、Webサイトの用意も不要です。
以下の4ステップだけで使えます。同じ手順は、アプリ内の「配信連携（OBS）」画面にも
そのまま載っています。

## 1. OBS用ファイルを書き出す

アプリの「配信連携（OBS）」画面を開き、「OBS用ファイルを書き出す」を押す。
あなた専用のオーバーレイHTMLファイルがその場で作られ、共有シートが開きます。

## 2. OBSを使うパソコンに送る

共有シートから「AirDrop」でMacに送る、または「ファイルに保存」して
iCloud Drive経由でパソコンへ、などお好きな方法で送ってください。

## 3. OBSの「ローカルファイル」で指定する

OBSの「ソース」→「＋」→「ブラウザ」→「ローカルファイル」にチェックを入れて、
送ったファイルを選ぶ。幅420・高さ160程度でOK。透過背景なので好きな位置に配置できます。

## 4. 配信中の使い方

配信前にアプリで「配信を開始」を押す（その時点から勝敗を数え直します）
→ 試合ごとにいつも通り「戦績登録」で記録 → 自動でOBSに反映される
→ 終わったら「配信を終了」。

---

ファイルを誰かに知られてしまった場合は、アプリの「トークンを再発行」で
今のファイルを無効化し、「OBS用ファイルを書き出す」から作り直してOBSに送り直してください。
