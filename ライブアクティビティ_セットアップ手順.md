# ライブアクティビティ セットアップ手順

「今日 〇勝〇敗」をロック画面／Dynamic Islandに常時表示する機能です。
アプリ本体側のコード（集計・開始・更新）は同梱済みですが、**実際の見た目を描画する
「Widget Extension」という別ターゲットだけは、Xcodeで一度だけ手動で追加する必要が
あります**（この部分はテキストファイルの追加だけでは自動化できないため）。

追加済みのファイル（アプリ本体側・作業不要）:

- `BongaRecord/Storage/BattleStatsSnapshot.swift`
- `BongaRecord/Storage/BongaRecordActivityAttributes.swift`
- `BongaRecord/Storage/LiveActivityManager.swift`
- `BongaRecord/Views/LiveActivitySettingsView.swift`（「設定」→「ライブアクティビティ」から開ける）
- `Info.plist`相当のビルド設定に `NSSupportsLiveActivities = YES` を追加済み

これから追加してもらうファイル（プロジェクト直下に同梱）:

- `BongaRecordWidgetLiveActivity.swift`

## 手順

1. Xcodeでプロジェクトを開き、File →New → Target を選択。
2. 「Widget Extension」を選び、Product Name は例えば `BongaRecordWidget` とする。
   「Include Live Activity」のチェックボックスがあれば **ON** にする（無い場合は後述の代替手順へ）。
3. 「Activate scheme?」と聞かれたら「Activate」を選ぶ。
4. 新しく作られた `BongaRecordWidget` フォルダの中に、Xcodeが自動生成したLive Activity用の
   Swiftファイル（`BongaRecordWidgetLiveActivity.swift` のような名前）があるはずです。
   その中身を、同梱の `BongaRecordWidgetLiveActivity.swift` の中身で置き換えてください
   （ファイル名が衝突する場合は、自動生成された方を削除してから同梱ファイルをドラッグ＆
   ドロップで追加）。
5. 同じフォルダに自動生成された `〇〇Bundle.swift`（`@main struct 〇〇Bundle: WidgetBundle`）を開き、
   `body` の中に `BongaRecordWidgetLiveActivity()` が含まれていることを確認する
   （テンプレートが別名で生成していたら、型名を `BongaRecordWidgetLiveActivity` に合わせて直す）。
6. プロジェクトナビゲータで `BongaRecordActivityAttributes.swift`
   （`BongaRecord/Storage/`にある方）を選択 → 右側のFile Inspectorの
   「Target Membership」で、アプリ本体に加えて **新しく作ったWidget Extensionにもチェック**
   を入れる（これを忘れると「型が見つからない」エラーになります）。
7. アプリ本体のスキームを選び直してビルド・実機（またはシミュレータ）で実行。
8. アプリ内「設定」→「ライブアクティビティ」→トグルをON。
9. 試合を1件登録すると、ロック画面／Dynamic Islandに「今日の勝敗」が出るはずです。

## 「Include Live Activity」のチェックボックスが無い場合

Xcodeのバージョンによっては表示されないことがあります。その場合はチェックなしで
Widget Extensionを作成し、手順4〜6は同じように進めてください（動作上の違いはありません。
チェックボックスは単にサンプルコードを追加生成してくれる補助機能です）。

## うまく表示されないとき

- 「設定」アプリ →「Face IDとパスコード」（機種によっては「画面表示と明るさ」）→
  「ライブアクティビティ」がONになっているか確認。
- アプリ内「設定」→「ライブアクティビティ」画面の「状態」欄にエラーが出ていないか確認。
- 手順6のTarget Membership設定漏れが最も多い原因です。
